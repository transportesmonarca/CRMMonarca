import type { ReactNode } from "react"

export default function SubirFotosEmbarqueLayout({ children }: { children: ReactNode }) {
    return <div className="min-h-screen bg-gray-50 p-4" > {children} </div>
}